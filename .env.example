# Auth (Neon Auth)
# Provided by Neon Console (Settings -> Auth) OR injected automatically for
# Vercel previews when using the Neon <-> Vercel integration with Auth enabled.
# Vercel OAuth callback URL must match:
#   <NEON_AUTH_BASE_URL>/callback/vercel
NEON_AUTH_BASE_URL=
# Optional: share cookies across subdomains (e.g. ".example.com")
NEON_AUTH_COOKIE_DOMAIN=
# App-side HMAC secret used to sign cached session data cookies (32+ chars).
NEON_AUTH_COOKIE_SECRET=

# App access policy (cost control)
# "restricted": only allow authenticated users whose email is in AUTH_ALLOWED_EMAILS
# "open": allow any authenticated user (use only after BYOK is implemented)
AUTH_ACCESS_MODE=restricted
# Comma-separated list of allowed emails (case-insensitive) when restricted
AUTH_ALLOWED_EMAILS=
# Comma-separated list of social providers to show in the auth UI (browser-exposed).
# - unset: defaults to "github,vercel"
# - empty string: disables social providers
# Recommended:
# - local/development: vercel
# - production: github,vercel
# - Vercel Preview branches: (empty) disable social providers to avoid per-branch
#   Vercel OAuth callback URL allowlisting (Neon Auth uses a branch-specific callback URL).
NEXT_PUBLIC_AUTH_SOCIAL_PROVIDERS=vercel

# Database (Neon Postgres)
DATABASE_URL=

# Upstash Redis (cache + rate limit)
UPSTASH_REDIS_REST_TOKEN=
UPSTASH_REDIS_REST_URL=

# Upstash Vector (semantic search)
UPSTASH_VECTOR_REST_TOKEN=
UPSTASH_VECTOR_REST_URL=

# Upstash QStash (durable workflows)
# Required to verify incoming QStash requests.
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=
QSTASH_TOKEN=

# Vercel AI Gateway (OpenAI-compatible)
AI_GATEWAY_API_KEY=
AI_GATEWAY_BASE_URL=https://ai-gateway.vercel.sh/v1
# Optional: default model IDs for the app (AI Gateway model IDs)
AI_GATEWAY_CHAT_MODEL=openai/gpt-4o-mini
AI_GATEWAY_EMBEDDING_MODEL=openai/text-embedding-3-small

# Vercel Blob (uploads)
BLOB_READ_WRITE_TOKEN=

# Web research
EXA_API_KEY=
FIRECRAWL_API_KEY=

# Vercel Sandbox (Code Mode)
VERCEL_OIDC_TOKEN=
# Required for Vercel Sandbox access-token auth mode (typically local dev/CI)
VERCEL_PROJECT_ID=

# MCP / Context7 (optional: only if your MCP transport needs a key)
CONTEXT7_API_KEY=

# -------------------------------
# Implementation / deploy automation (optional)
# -------------------------------

# GitHub RepoOps
GITHUB_TOKEN=
# Optional: GitHub webhooks signature verification
GITHUB_WEBHOOK_SECRET=

# Optional: Neon API for auto-provisioning target app DB
NEON_API_KEY=

# Optional: Upstash Developer API for auto-provisioning resources
# (Only available for native Upstash accounts)
UPSTASH_API_KEY=
UPSTASH_EMAIL=

# Vercel REST API / SDK (deployment automation)
# Optional: team scope
VERCEL_TEAM_ID=
VERCEL_TOKEN=
