# Auth (single-user)
ADMIN_PASSWORD_HASH=
# At least 32 chars. Rotating invalidates existing sessions.
APP_SESSION_SECRET=

# Database (Neon Postgres)
DATABASE_URL=

# Upstash Redis (cache + rate limit)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Upstash Vector (semantic search)
UPSTASH_VECTOR_REST_URL=
UPSTASH_VECTOR_REST_TOKEN=

# Upstash QStash (durable workflows)
QSTASH_TOKEN=
# Required to verify incoming QStash requests.
QSTASH_CURRENT_SIGNING_KEY=
QSTASH_NEXT_SIGNING_KEY=

# Vercel AI Gateway (OpenAI-compatible)
AI_GATEWAY_API_KEY=
AI_GATEWAY_BASE_URL=https://ai-gateway.vercel.sh/v1

# Vercel Blob (uploads)
BLOB_READ_WRITE_TOKEN=

# Web research
EXA_API_KEY=
FIRECRAWL_API_KEY=

# Vercel Sandbox (Code Mode)
VERCEL_OIDC_TOKEN=

# MCP / Context7 (optional: only if your MCP transport needs a key)
CONTEXT7_API_KEY=
