# UI Components Test Suite

Store all `components/ui` tests in this directory.

- Use `*.test.ts` or `*.test.tsx` filenames.
- Keep tests close to shared primitive behavior and accessibility contracts.
- Add regression tests when changing focus, keyboard, labeling, or form semantics.
