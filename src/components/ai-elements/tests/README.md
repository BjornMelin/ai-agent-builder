# AI Elements Test Suite

Store all `ai-elements` component tests in this directory.

- Use `*.test.ts` or `*.test.tsx` filenames.
- Keep test names aligned to exported component names.
- Prefer focused accessibility and interaction regression tests.
